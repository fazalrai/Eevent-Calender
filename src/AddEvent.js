import React from 'react'
import Button from '@material-ui/core/Button';
function AddEvent() {

  return (
    <Button style={{position: "absolute", bottom: "-100", marginTop: "10px"}}  variant="contained" color="primary">
    Add Event
  </Button>
  )
}

export default AddEvent
